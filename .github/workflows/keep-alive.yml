name: keep-alive

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render health endpoints
        run: |
          curl -fsS --max-time 25 https://mediqueue-1cu4.onrender.com/api/health || true
          curl -fsS --max-time 25 https://mediqueue-hi0e.onrender.com/api/health || true