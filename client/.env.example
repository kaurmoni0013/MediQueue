# MediQueue client environment
# Copy to .env.local to override.
# In development the dev server proxies /api to the backend (port 5000),
# so you normally do not need to change this.
VITE_API_URL=/api